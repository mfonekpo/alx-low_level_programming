#include "main.h"
#include <string.h>

/**
 * _strcat - str concat
 * @dest: destination
 * @src: str source
 * Return: strcat
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
