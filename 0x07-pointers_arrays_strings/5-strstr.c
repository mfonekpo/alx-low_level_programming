#include "main.h"
#include <string.h>

/**
 * _strstr - strstr
 * @haystack: haystack
 * @needle: needle
 * Return: returns a list
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
